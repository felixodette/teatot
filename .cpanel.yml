---
# cPanel Git → build on pull → public_html/development
# cPanel → Git Version Control → branch develop → enable Pull and Deploy
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/development
    - /bin/bash scripts/cpanel-deploy.sh
